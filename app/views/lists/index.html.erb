<div class="container">
  <div class="row">
    <div class="col-12">
    <%= link_to "Crear nueva Lista de Tareas", new_list_path, class: "btn btn-primary" %>
      <ul>
        <% @lists.each do |list| %>
          <li class="listItem"><%= link_to list.name, list_path(list) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
